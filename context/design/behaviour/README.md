# behaviour/

Expected system behaviours and measurement practices.

- Captures **what must happen** at runtime (requirements/invariants) to keep results comparable and reproducible.
- Stays **implementation-agnostic** (avoid code-level details); link to specs/runbooks for concrete commands when needed.

